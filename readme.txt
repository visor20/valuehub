stocks == Object
must use .keys or .values on this object before using the .map function.

Additionally, Flask seems to be sorting the JSON by default... 
It looks like disabling this behavior is possible, but the questions remains: why is this the default?

